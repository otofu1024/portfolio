# AGENTS.md

このリポジトリは、TypeScript + Vite + Reactで構成されたミニマルなポートフォリオサイトです。

## AIエージェント向けのルール

- コメントを書く際は必ず日本語を使用すること。
- ユーザーから明示的なコード変更の指示がない限り、実装ファイルに変更を加えないこと。
- サイトはミニマルに保つこと：グラデーション、カードを多用したUI、過剰な影の付与、および不要な装飾は禁止。ダークモードは既存の手動切り替え実装を尊重すること。
- 1ページ構成を維持すること（Hero、About、Skills、Projects、Contactの順）。
- 新たな依存関係を追加するよりも、シンプルなセマンティックHTMLとTailwind CSSのユーティリティクラスを優先すること。
- 要請がない限り、ルーティング、フォーム送信機能、アニメーションライブラリ、CMSロジック、バックエンドコードは追加しないこと。
- タスクで明示的に求められない限り、`src/main.tsx`、`vite.config.ts`、`package.json`は変更しないこと。
- `src/App.tsx`を表示データおよびページ構造の現在のソースとして扱うこと。
- `src/App.tsx`内のTailwind CSSクラスと`src/index.css`を現在のスタイリングレイヤーとして扱うこと。
- ドキュメント作成のみのタスク中に改善点を見つけた場合は、コードを変更するのではなく、その内容をドキュメントに記載すること。

## 検証

実装に変更を加えた後は、以下のチェックを実行すること：

```bash
npm run lint
npm run build
```

手動での確認には開発用サーバーを使用すること：

```bash
npm run dev
```
